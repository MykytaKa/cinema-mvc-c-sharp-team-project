@model BookingViewModel

<h2>Change Booking Status</h2>

<table class="table table-bordered">
    <thead>
        <tr>
            
            <th>User</th>
            <th>Film</th>
            <th>Session Time</th>
            <th>Current Status</th>
            <th>New Status</th>
            
        </tr>
    </thead>
    <tbody>
        @foreach (var booking in Model.AllBookings)
        {
            <tr>
               
                <td>@booking.User.FirstName @booking.User.LastName</td>
                <td>@booking.Session.Film.Name</td>
                <td>@booking.Session.DateTimeBeg - @booking.Session.DateTimeEnd</td>
                <td>@booking.Status.Name</td>
                <td>
                    <form asp-action="ChangeBookingStatus" method="post" class="d-flex">
                        <input type="hidden" name="bookingId" value="@booking.Id" />
                        <select name="newStatusId" class="form-control me-2">
                            @foreach (var status in Model.AvailableStatuses)
                            {
                                <option value="@status.Id" selected="@(booking.Status?.Id == status.Id ? "selected" : null)">
                                    @status.Name
                                </option>
                            }
                        </select>
                        <button type="submit" class="btn btn-primary">Confirm</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>

@if (Model.TotalPages > 1)
{
    <div>
        <nav>
            <ul class="pagination">
                @for (int i = 1; i <= Model.TotalPages; i++)
                {
                    <li class="page-item @(i == Model.CurrentPage ? "active" : "")">
                        <a class="page-link" asp-action="ChangeBooking" asp-route-page="@i">@i</a>
                    </li>
                }
            </ul>
        </nav>
    </div>
}
